# Vercel/serverless runtime (keep bundle under platform limit).
# Full quant stack (scipy, scikit-learn, hmmlearn, statsmodels) is in repo-root requirements.txt for local/CI.
fastapi
uvicorn
pydantic
yfinance
pandas
numpy
python-dotenv
fredapi
httpx
